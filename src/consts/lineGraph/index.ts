export const svgWidth = 320;
export const svgHeight = 240;
export const dataSet = [10, 47, 65, 8, 64, 99, 75, 22, 63, 80];
export const margin = svgWidth / (dataSet.length - 1); // 꺾은선 그래프 간격 계산

export const offsetX = 30; // 가로 오프셋
export const offsetY = 20; // 세로 오프셋
export const scale = 2.0; // 2배로
